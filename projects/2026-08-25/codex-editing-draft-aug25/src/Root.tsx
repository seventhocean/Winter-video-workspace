import {Composition} from "remotion";
import {Main} from "./Main";

export const Root = () => (
  <Composition
    id="Main58"
    component={Main}
    durationInFrames={4059}
    fps={30}
    width={2160}
    height={4678}
    defaultProps={{sourceFile: "draft-render.mov"}}
  />
);
